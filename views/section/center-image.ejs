<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= section.title %> - Center Image Section</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
            background-color: #f5f5f5;
        }

        .image {
            max-width: 70%; /* Adjust the max-width as needed */
        }

        img {
            max-width: 100%;
            height: auto;
            display: block;
            margin: 0 auto; /* Center the image horizontally */
        }

        .content {
            text-align: center;
            margin-top: 20px; /* Adjust the margin as needed */
        }
    </style>
</head>
<body>

<div class="container">
    <div class="image">
        <!-- Display the image -->
        <img src="<%= section.image %>" alt="Image">
    </div>
    <div class="content">
        <!-- Your content goes here -->
        <h1><%= section.title %></h1>
        <p><%= section.subHeading %></p>
        <!-- Add more content as needed -->
    </div>
</div>
</body>
</html>
